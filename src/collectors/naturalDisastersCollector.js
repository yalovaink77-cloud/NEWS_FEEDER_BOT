const axios = require('axios'); const fetch = require('node-fetch'); async function getUSGSEarthquakes() { try { const response = await axios.get('https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/significant_month.geojson'); console.log('✓ USGS Earthquakes fetched'); return response.data.features.map(e => ({ source: 'USGS', title: e.properties.title, magnitude: e.properties.mag, location: e.properties.place })); } catch (error) { console.error('✗ USGS error:', error.message); return []; } } async function getEMSCData() { try { const response = await fetch('https://www.emsc-csem.org/service/rss/rss.php?limit=20'); console.log('✓ EMSC Earthquakes fetched'); return await response.text(); } catch (error) { console.error('✗ EMSC error:', error.message); return []; } } async function getNOAAWeather() { try { const response = await fetch('https://www.ncei.noaa.gov/access/monitoring/climate-at-a-glance/global/time-series/rss.xml'); console.log('✓ NOAA Weather fetched'); return await response.text(); } catch (error) { console.error('✗ NOAA error:', error.message); return []; } } async function getNaturalDisasters() { console.log('Collecting Natural Disasters...'); const usgs = await getUSGSEarthquakes(); const emsc = await getEMSCData(); const noaa = await getNOAAWeather(); return { usgs, emsc, noaa }; } module.exports = { getNaturalDisasters };